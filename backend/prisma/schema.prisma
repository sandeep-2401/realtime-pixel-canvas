datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Pixel {
  roomId String
  x     Int
  y     Int
  color String

  @@id([roomId, x, y])
}

